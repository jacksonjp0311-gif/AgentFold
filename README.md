# AgentFold: Evidence-Gated Agent Folding Runtime

## Repository Description

AgentFold is a local-first Python reference runtime for modeling how a tool-using agent moves from declared operating substrate to observed behavior.

It adapts the Observable Manifold Network README shell into an agent-governance context:

1. AgentFold runtime: genome schema, origin certificates, transcript capture, fold graph construction, fold prediction, misfold detection, fitness scoring, compounding gates, fold certificates, ledgers, and inheritance controls.
2. RCC-N navigation: Human Director Box, README trisection, repository context indexes, route maps, mini README Echo Location records, and validation-bound AI operating protocol.
3. Codex documentation shell: software architecture boundary, rehydration protocol, evidence surfaces, non-claim locks, deterministic run guidance, and folder-level local context.

Boundary: this repository improves agent traceability, local validation, and repository orientation. It does not prove code correctness, security, production readiness, AGI, consciousness, biological equivalence, autonomous authority, claim truth, or safe real-world action.

> AgentFold tests whether an agent run can declare its genome, align to origin, execute under trace, build a behavioral fold graph, detect misfolds, score fitness, gate compounding, write an append-only ledger, and admit only validated deltas into inheritance.

## Human Director Box

### What is this?

AgentFold is a governed software workbench for sequence-to-behavior inspection in memory-bearing, tool-using agents. It treats prompts, memory, tools, permissions, constraints, evidence, environment, prior state, and lineage as the operational genome from which runtime behavior is expressed.

### What changed?

This update mirrors the OMN/AERMA README trisection and RCC-N navigation lessons into AgentFold:

- PART I - Human README
- PART II - RCC Nexus README
- PART III - AI Agent README

It also adds a 90-second README, a rehydration protocol, repository context indexes, a route map, folder-level mini READMEs, and validation tests for the documentation shell. The codebase was also repaired so the CLI and tests work before the new docs claim health.

### Current health snapshot

| Surface | Current result |
|---|---:|
| Package / CLI | `agentfold` |
| Current software layer | AF-SA v1.4.0 |
| Previous seal | AF-SA v0.1.0 genesis architecture |
| README shell | Human / RCC Nexus / AI Agent trisection |
| Mini README coverage | enforced by test |
| Rehydration protocol | `docs/protocols/REHYDRATION_PROTOCOL.md` |
| Genesis architecture source | `docs/software_architecture/agentfold_sa_v0_1_0_genesis_source.md` |
| Cross-domain analysis | `docs/cross_domain/alphafold_genome_rna_agentfold_analysis.md` |
| AgentPhenotype runtime surface | `agentfold/folding/phenotype.py` |
| Authority matrix | `agentfold/authority/matrix.py` |
| Evidence packet replay | `agentfold/evidence/packet.py` |
| Evidence report renderer | `agentfold/reports/markdown.py` |
| Schema exporter | `agentfold/schemas/export.py` |
| Runtime pipeline | `agentfold/runtime/pipeline.py` |
| Instrumented session API | `agentfold/instrumentation/session.py` |
| Guarded tool wrapper | `agentfold/instrumentation/tool_guard.py` |
| CI verifier | `agentfold/ci/verify.py` |
| Policy file | `agentfold.policy.json` |
| Misfold fixtures | `benchmarks/misfold_fixtures/` |
| Release validator | `agentfold/release/validator.py` |
| Docs registry | `docs/DOCS_REGISTRY.md` |
| Release candidate manifest | `releases/agentfold_v1_4_0_release_candidate.json` |
| Release functional | `agentfold/release/functional.py` |
| v1.0.0 release seal | `releases/agentfold_v1_0_0_local_release_seal.json` |
| Evolution roadmap | `docs/roadmap/agentfold_evolution_roadmap.md` |
| Repository context index | `docs/context/repository_context_index.json` |
| RCC Nexus index | `docs/context/rcc_nexus_index.json` |
| Route map | `rcc/nexus/route_map.json` |
| CLI command group | repaired |
| Compounding gate tests | repaired |
| Misfold enum handling | repaired |
| Origin certificate default | repaired |
| Ledger writer continuation | repaired |
| Current tests | 87 passed |
| Claim status | local scaffold validated |
| Non-claim lock | active |

### What this is not

- Not biological AlphaFold.
- Not a biological model.
- Not genetic engineering.
- Not RNA design.
- Not protein folding.
- Not a wetlab system.
- Not AGI.
- Not consciousness.
- Not proof of truth.
- Not proof of safety.
- Not proof of security.
- Not production validation.
- Not autonomous authority.
- Not write authority.
- Not a replacement for human permission.

### Where do I start?

1. Read this README.
2. Read `README_90_SECONDS.md`.
3. Open `docs/protocols/REHYDRATION_PROTOCOL.md`.
4. Open `docs/context/repository_context_index.json`.
5. Open `docs/context/rcc_nexus_index.json`.
6. Open `rcc/nexus/route_map.json`.
7. Run `python -m pytest -q`.
8. Run `python -m agentfold.cli --help`.
9. Run `python -m agentfold.cli validate-genome examples/genomes/minimal_agent_genome.json`.

---

## Current AF-SA v1.4.0 Release Candidate Validation Checkpoint

v1.4.0 evolves AgentFold from CI gatekeeping into a release candidate validation surface. It keeps CI policy, misfold fixtures, instrumentation, persisted evidence, replay, reports, schemas, and the local pipeline, then adds a release validator, docs registry, and release candidate manifest.

Current measured state:

| Surface | Result |
|---|---:|
| Unit tests | 87 passed |
| CLI help | available |
| Genome validation example | available |
| Rehydration protocol | added |
| Route map | added |
| Context indexes | added |
| Mini README coverage | added |
| AgentPhenotype | implemented |
| Authority matrix | implemented |
| Replayable evidence packet | implemented |
| Evidence packet persistence | implemented |
| Evidence replay CLI | implemented |
| Evidence diff CLI | implemented |
| Markdown evidence reports | implemented |
| JSON Schema export | implemented |
| Session recorder API | implemented |
| Guarded tool wrapper | implemented |
| Adapter contract | implemented |
| Session CLI lifecycle | implemented |
| CI policy | implemented |
| CI verifier | implemented |
| CI summaries | implemented |
| Misfold fixture pack | implemented |
| GitHub Actions template | implemented |
| Release validator | implemented |
| Docs registry | implemented |
| Release candidate manifest | implemented |
| Full local pipeline | implemented |
| Release functional | implemented |
| Non-claim lock | preserved |

v1.4.0 law:

    No origin alignment, no compounding.
    No transcript, no inheritance.
    No fold graph, no fold prediction claim.
    No fold evidence, no phenotype claim.
    No replayable evidence packet, no mature fold claim.
    No persisted packet, no portable replay.
    No schema, no integration contract.
    No instrumented events, no real transcript claim.
    No guard wrapper, no tool execution claim.
    No CI verification, no automated merge gate claim.
    No misfold fixture, no adversarial coverage claim.
    No release validation, no release candidate claim.
    No docs registry, no release orientation claim.
    No authority matrix pass, no mutation surface.
    No release functional, no release checkpoint.
    No evidence map, no claim elevation.
    No route map, no agent-safe patch.
    No ledger continuity, no durable memory promotion.

Boundary:

These changes improve repository orientation, local testability, and claim discipline. They do not prove code correctness, patch safety, empirical validation, production readiness, AGI, consciousness, or safe autonomous action.

## AgentFold Natural Evolution

Evolution law:

    Rehydrate the origin.
    Measure the missing surface.
    Repair the drift.
    Validate the repair.
    Write the ledger.
    Preserve the boundary.

The OMN lesson applied here is simple: a repository that asks agents to operate safely must expose its own map, current health, route surfaces, validation commands, and claim boundaries.

## Genesis Source Alignment

The original AF-SA v0.1.0 gist is now treated as the canonical architecture source for future evolution:

    https://gist.github.com/jacksonjp0311-gif/2d92eb0f12bba782639460f18a426f42

AgentFold v0.1.1 implements only the local scaffold and repository geometry around that source. The next evolution path is to move more of the genesis architecture from prose into validated runtime surfaces: replayable evidence packets, phenotype records, fold certificates, release functionals, authority locks, and transcript-to-ledger inheritance.

Boundary: source alignment preserves intent and traceability. It does not prove that every architectural concept is implemented yet.

## Cross-Domain Evolution Layer

AgentFold now includes a bounded cross-domain analysis from AlphaFold, genome/RNA expression, and RNA folding:

    docs/cross_domain/alphafold_genome_rna_agentfold_analysis.md
    docs/cross_domain/cross_domain_mapping.json

The analysis extracts software lessons only:

- sequence is not behavior,
- expression intermediates matter,
- fold prediction is contextual,
- confidence is not validation,
- misfolds are structural outcomes,
- fitness must stay bounded and multi-dimensional.

Runtime evolution from this layer:

- `AgentPhenotype` is now implemented as a software behavior summary derived from fold evidence.

Boundary: biology supplies architecture metaphors and cautionary constraints. AgentFold does not model proteins, DNA, RNA, molecular complexes, wetlab systems, AGI, consciousness, or biological equivalence.

# PART I - Human README

## Current Identity

AgentFold is a local Python package for testing whether agent behavior can be bounded by declared substrate and evidence:

- declare an AgentGenome,
- certify origin alignment,
- capture runtime expression events,
- build an AgentTranscriptome,
- construct a BehavioralFoldGraph,
- predict risk before or during folding,
- detect misfolds,
- evaluate fitness,
- apply compounding gates,
- emit fold certificates,
- append ledger records,
- block unsafe inheritance,
- preserve non-claim boundaries.

The current repo is a scaffold with working local tests. It is not a production agent runtime.

## Quick Start

Install locally:

    pip install -e .

Run CLI help:

    python -m agentfold.cli --help

Validate the example genome:

    python -m agentfold.cli validate-genome examples/genomes/minimal_agent_genome.json

Run the dry-run pipeline:

    python -m agentfold.cli run examples/genomes/minimal_agent_genome.json --task examples/tasks/memory_drift_task.json

Run tests:

    python -m pytest -q

## What AgentFold Tests

| Task | Purpose |
|---|---|
| Genome validation | Ensures declared agent substrate has required IDs, claim ceiling, tools, permissions, and evidence boundaries. |
| Origin alignment | Blocks compounding when the session cannot anchor to declared origin. |
| Transcript capture | Records runtime expression events before inheritance is allowed. |
| Fold graph construction | Converts observed events into a causal/semantic graph. |
| Misfold detection | Detects origin drift, claim overreach, evidence gaps, and unsafe compounding signals. |
| Compounding gate | Permits, warns, repairs, shadows, blocks, or requires human review. |
| Ledger continuity | Writes append-only JSONL entries with hash linkage. |
| Inheritance controls | Promotes only validated deltas. |

AgentFold rewards bounded evidence emission and durable auditability, not confident overclaiming.

## Current Hardening Layer

The current hardening layer includes:

- README trisection,
- 90-second orientation file,
- rehydration protocol,
- route map and context indexes,
- mini README Echo Location records,
- working Click CLI,
- repaired tests,
- enum-safe misfold events,
- hashchain continuation across writer instances,
- origin certificate construction compatible with tests,
- explicit non-claim lock.

## Project Structure

    agentfold/
      README.md                         # Human / RCC Nexus / AI trisection
      README_90_SECONDS.md              # Short adoption compression
      pyproject.toml                    # Package metadata and CLI entry
      agentfold/                        # Python package
        genome/                         # AgentGenome schema, loading, validation, hashing
        origin/                         # Origin certificates, alignment, deviation
        transcriptome/                  # Runtime trace capture and transcript schema
        expression/                     # Runtime expression events
        folding/                        # Fold graph, prediction, misfolds, certificates
        gates/                          # Origin, evidence, permission, claim, replay, ledger, compounding gates
        fitness/                        # Fitness scorecards and evaluator
        ledger/                         # JSONL append-only ledger and hashchain verification
        inheritance/                    # Delta validation, blocking, promotion
        benchmark/                      # Benchmark task scaffolding
        adapters/                       # Codex, CMS, Tessera, RHP, GNB adapters
      docs/
        context/                        # Repository and RCC Nexus indexes
        cross_domain/                   # Bounded AlphaFold/genome/RNA analysis
        protocols/                      # Rehydration and operating protocols
        roadmap/                        # Evolution roadmap and implementation priorities
        software_architecture/          # Source-bound AgentFold architecture records
      rcc/nexus/                        # Route maps and navigation shell
      examples/
        genomes/                        # Example AgentGenome files
        tasks/                          # Example task definitions
      tests/                            # Local validation suite

## Canonical Runtime Law

    No origin alignment, no compounding.
    No agent genome, no execution.
    No transcript capture, no inheritance.
    No fold graph, no fold prediction claim.
    No evidence map, no claim elevation.
    No permission manifest, no tool authority.
    No claim ceiling, no public claim.
    No misfold check, no durable mutation.
    No ledger, no memory promotion.
    No replay, no validation claim.
    No human authorization, no high-consequence action.
    No gate pass, no compounding.

## Rehydration Protocol

AgentFold rehydration is the process of restoring local context before an agent makes code, claim, or inheritance changes.

Canonical sequence:

    anchor -> read -> index -> route -> validate -> inspect -> plan -> patch -> test -> ledger -> boundary

Minimum local sequence:

1. Anchor to repo root.
2. Read `README.md`.
3. Read `README_90_SECONDS.md`.
4. Read `docs/protocols/REHYDRATION_PROTOCOL.md`.
5. Read `docs/context/repository_context_index.json`.
6. Read `docs/context/rcc_nexus_index.json`.
7. Read `rcc/nexus/route_map.json`.
8. Inspect `git status --short --branch`.
9. Run or preserve the declared validation command.
10. Keep all claims inside the non-claim lock.

## Evidence and Validation Surfaces

| Surface | Command or path |
|---|---|
| Test suite | `python -m pytest -q` |
| CLI help | `python -m agentfold.cli --help` |
| Example genome validation | `python -m agentfold.cli validate-genome examples/genomes/minimal_agent_genome.json` |
| Rehydration protocol | `docs/protocols/REHYDRATION_PROTOCOL.md` |
| Genesis source summary | `docs/software_architecture/agentfold_sa_v0_1_0_genesis_source.md` |
| Cross-domain mapping | `docs/cross_domain/cross_domain_mapping.json` |
| Evolution roadmap | `docs/roadmap/agentfold_evolution_roadmap.md` |
| Repository context index | `docs/context/repository_context_index.json` |
| RCC route map | `rcc/nexus/route_map.json` |

## Lessons Imported From OMN

| OMN lesson | AgentFold adaptation |
|---|---|
| README trisection improves human and agent orientation. | AgentFold now separates human, RCC Nexus, and AI Agent surfaces. |
| Mini READMEs turn folders into local context cells. | Every non-generated subfolder has a README. |
| No route map, no agent-safe patch. | `rcc/nexus/route_map.json` declares the patch routes. |
| CI and release validation must not be confused. | AgentFold currently reports local tests only; no production or release claim is made. |
| Evidence packages mature through replay and drift checks. | AgentFold records this as a planned evolution for ledgers, transcript fixtures, and fold certificates. |
| Version surfaces need automated checks. | A documentation coverage test now protects the context shell. |
| The original architecture must stay source-bound. | AgentFold now tracks the AF-SA v0.1.0 gist as the genesis source. |

# PART II - RCC Nexus README

## RCC Nexus Purpose

The RCC Nexus layer makes AgentFold orientable to humans and AI agents by exposing:

- repository identity,
- route maps,
- folder responsibilities,
- validation surfaces,
- non-claim boundaries,
- rehydration order,
- safe patch routes.

## Nexus Entry Points

| Entry | Purpose |
|---|---|
| `docs/context/repository_context_index.json` | Machine-readable repository map. |
| `docs/context/rcc_nexus_index.json` | RCC-N navigation and governance index. |
| `rcc/nexus/route_map.json` | Route map from intent to files and validation. |
| `docs/protocols/REHYDRATION_PROTOCOL.md` | Agent rehydration and safe-work protocol. |

## Echo Location Block

Repository self-location:

    repo: agentfold
    layer: AF-SA v1.4.0
    role: evidence-gated agent folding scaffold
    primary package: agentfold
    validation: python -m pytest -q
    boundary: local scaffold validation only

## RCC-N Boundary

RCC-N improves navigation and patch safety. It does not prove:

- code correctness,
- security,
- empirical validity,
- production readiness,
- AI understanding,
- agent autonomy,
- truth of claims,
- biological equivalence.

# PART III - AI Agent README

## Required Read Order

1. `README.md`
2. `README_90_SECONDS.md`
3. `docs/protocols/REHYDRATION_PROTOCOL.md`
4. `docs/context/repository_context_index.json`
5. `docs/context/rcc_nexus_index.json`
6. `rcc/nexus/route_map.json`
7. Local folder README for the files being changed

## Agent Operating Rules

- Preserve user changes.
- Inspect `git status --short --branch` before edits.
- Do not promote claims beyond local validation.
- Do not imply AGI, consciousness, biological equivalence, production readiness, or autonomous authority.
- Keep edits scoped to the route being touched.
- Run `python -m pytest -q` before claiming the repo is healthy.
- Update mini READMEs when adding folders.
- Update context indexes when adding major surfaces.

## Patch Routes

| Intent | Route |
|---|---|
| Genome/schema changes | `agentfold/genome/`, then genome tests |
| Origin/rehydration changes | `agentfold/origin/`, `docs/protocols/`, then origin tests |
| Runtime transcript changes | `agentfold/transcriptome/`, `agentfold/expression/`, then transcript tests |
| Fold graph/misfold changes | `agentfold/folding/`, `agentfold/gates/misfold_gate.py`, then folding/misfold tests |
| Gate changes | `agentfold/gates/`, then gate and compounding tests |
| Ledger changes | `agentfold/ledger/`, then ledger tests |
| Documentation shell changes | `README.md`, `README_90_SECONDS.md`, `docs/context/`, `rcc/nexus/`, folder READMEs, then documentation tests |

## AI README Update Policy

Every structural change should update three surfaces:

1. Human explanation if user-facing behavior changes.
2. RCC route/index files if navigation changes.
3. Folder README if local responsibility changes.

Boundary: README updates improve orientation. They do not substitute for tests, review, or evidence.

## Author

James Paul Jackson - @unifiedenergy11

## Date

June 2026

## Status

AF-SA v1.4.0 release candidate validation checkpoint.
